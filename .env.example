# Required configuration
AIRPLANES_LAT=38.0375
AIRPLANES_LON=-78.4863
AIRPLANES_RADIUS=2.5

# How often to poll airplanes.live in seconds
POLL_INTERVAL_SECONDS=5

# Minimum time between notifications for the same aircraft
COOLDOWN_MINUTES=10

# Maximum altitude (ft) to track aircraft. Set to 0 or leave blank to disable filtering.
MAX_ALTITUDE_FT=25000

# Location of the persisted database (relative paths are resolved from the project root)
DATA_FILE=data/db.json

# Apprise configuration
# The API URL should point at an Apprise API server. When using the provided docker-compose
# stack the default value works out of the box.
APPRISE_API_URL=http://apprise:8000/notify
# Comma-separated or newline separated list of notification URLs.
# Example: discord://webhook_id/webhook_token
APPRISE_URLS=
# Optional: when using Apprise configuration keys, provide the key and leave APPRISE_URLS blank.
APPRISE_CONFIG_KEY=
